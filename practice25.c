#include <stdio.h>
#define SIZE 5
int main(){
	int Arr[SIZE]={10,20,30,40,50};
	int i;
	// Print(Display) array elements(value)
	for (i=0;i<SIZE;i++)
	{
		printf("\nArr[%d]=%d",i,Arr[i]);
	}
}
